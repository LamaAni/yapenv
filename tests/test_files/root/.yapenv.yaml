# From bole
settings:
  inherit: true

python_version: "3.9"
venv_directory: .venv

environments:
  test:
    requirements:
      - import: requirements.test.txt

requirements:
  - pyyaml==5.8
  - import: requirements.txt

test_val: "source"
