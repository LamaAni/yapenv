# Template document for .yapenv config

# Get current python version (executing)
venv_directory: .venv
requirements:
  - import: requirements.txt

environments:
  dev:
    requirements:
      - import: requirements.dev.txt
